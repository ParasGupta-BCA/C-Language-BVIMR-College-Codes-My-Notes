include<stdio.h>

    int fact();

int main()

    int i,
    fact = 1, number;

printf("Enter a nuber: "D scanf("%d", &number); for(i=1, i.<number, i++) 
{

fcat-facti
}
 printf ("Factorial of &d is: &d", number, fact);

return 0;
